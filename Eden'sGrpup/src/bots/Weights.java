package bots;
/**
 * 
 * @author yuval
 * @version 1.0
 *This interfaceis for have constant values for the ML stuff as the thing we had create here.
 *For example the weights for time and mana and by the way the bias's(the bounus points) too.
 */
public interface Weights {

	// constants(at version no. 1 the values are not checked, needs to run first of all)
	public static final double TIME_WEIGHT = 0.4;
	public static final double MANA_WEIGHT = 0.6;
	
	
}
